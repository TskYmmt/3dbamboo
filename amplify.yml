version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - echo "No build step needed for static files"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
backend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - echo "Backend build completed"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'